# Experimental Control Runtime Archive

This directory preserves the older proof-heavy `shift` direction:

- research packet
- control-lab witness surfaces
- workflow and showcase material
- additive driver-era files and tests

It is kept for historical reference only.
It is not part of the default build, default docs story, or recommended public API.
